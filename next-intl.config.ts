// next-intl (App Router) config
// This file is required so next-intl can discover routing locales at runtime.
// Keep it minimal and framework-compatible.

export default {
  locales: ["en", "tr"],
  defaultLocale: "en"
};

